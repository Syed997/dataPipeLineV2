apiVersion: v1
kind: Service
metadata:
  name: flask-otel-service
  namespace: flask-demo
spec:
  selector:
    app: flask-otel
  ports:
    - protocol: TCP
      port: 9001
      targetPort: 9001
  type: NodePort
